const slide = document.getElementsByClassName(".slide");
const leftBtn = document.getElementsByClassName(".btn-prev");
const rightBtn = document.getElementsByClassName(".btn-next");

let currentSlide = 0;
let maxCount = slide.length;
